---
applications:
  - buildpack: https://github.com/cloudfoundry/java-buildpack.git
    name: frontend
    memory: 2048m
    path: frontend-prebuild.jar
    random-route: true
    services:
      - config-service
      - discovery-service
    timeout: 180