# simple-frontend

## Build the app

Use the included maven wrapper:

```
./mvnw clean install
```

## Deploy the app

Verify I'm pointed to the right space:
```
cf target
```

Push the app:
```
cf push
```